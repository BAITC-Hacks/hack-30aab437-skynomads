import React from 'react';

const Home = () => (
  <article className={'page'}>
    <section className={'container-fluid'}>
      Building applications using Webpack, React
    </section>
  </article>
);

export default Home;
